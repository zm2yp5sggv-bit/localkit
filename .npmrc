# npm 配置
#
# engine-strict：让 package.json 的 engines 成为硬约束。
# 起因是一次真实故障——CI 把 Node 20 放进了版本矩阵，而 `node --test` 的 glob 参数
# 要到 Node 21 才支持，于是自测步骤报出的是「Could not find '.../**/*.test.mjs'」
# 这种指向错误的提示，排查了半天才发现是 Node 版本问题。
# 打开它之后，版本不符会在 `npm install` 阶段就以明确信息失败。
engine-strict=true
