# 安全策略

## 报告漏洞

请通过 **GitHub Security Advisories** 私下报告，不要开公开 Issue：

👉 https://github.com/zm2yp5sggv-bit/localkit/security/advisories/new

如果无法使用该入口，也可以开一个只写「需要私下联系」的 Issue，我会回复提供邮箱。

请尽量附上：复现步骤、受影响的页面与浏览器版本、以及你所判断的影响面。
这是个人维护的业余项目，我会尽力在一周内回复，但请理解无法承诺 SLA。

## 设计上需要知道的边界

LocalKit 是纯静态站点，**没有后端、没有账号、不接收任何上传**。这决定了很大一部分
常见漏洞类型在本项目里并不存在（服务端注入、认证绕过、越权、SSRF 等）。

真正有价值、也欢迎报告的问题类型：

- **数据外发**：任何导致访问者文件或数据被发送到本站之外的代码路径。
  本站明确承诺零第三方请求，这是核心卖点，属于最高优先级。
- **跨站脚本（XSS）**：把不可信字符串（文件名、用户输入、外部数据）拼接进 `innerHTML`。
  项目约定所有此类拼接必须经过 `LK.esc()`，但仍可能有遗漏。
- **本地数据泄露**：工具在用户不知情的情况下把文件内容写入持久化存储、剪贴板或跨页共享状态。
- **依赖完整性**：`assets/vendor/` 下的第三方构建产物被替换或篡改。
  这些文件随仓库提交并有 sha256 清单（`scripts/vendor.json`），`npm run check:vendor` 会校验。
- **CSP 绕过**：`_headers` 下发的策略被绕过，尤其是 `connect-src 'none'` 这条。

## 已知的非问题

- 站点使用 `localStorage` 保存一个语言偏好键 `lk-lang`。这是唯一写入的本地数据，且已用 `try/catch`
  兼容隐私模式与 `file://`。
- `<script>` 内的内联脚本需要 `'unsafe-inline'`，因此 CSP 里保留了该来源。
  这是「零构建、无打包器」这一设计取舍的结果，无法在不引入构建步骤的前提下消除。
- 站点不设置 Cookie，也不需要任何 Cookie 同意机制。

## 隐私承诺如何被验证

「文件不上传」不是一句文案，而是被三重约束固定下来的：

1. 代码层面：全部处理逻辑在浏览器内完成，页面不使用 `fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon`。
2. 策略层面：`_headers` 中的 `connect-src 'none'` 在浏览器层面禁止发起任何网络请求。
3. 测试层面：端到端测试会遍历全站页面并断言零第三方请求；`npm run check:syntax`
   会拒绝任何残留的 CDN 引用。

三者中任何一条被破坏，CI 都会失败。
